<% if current_user %>
  <div class="form-group">
  <%= form_for [list, @item] do |f| %>
    <br/>
    <%= f.text_field :name, class: 'form-control', placeholder: "Enter To-Do" %>
    </div>
    <div class="form-group">
    <%= f.submit "Save Item", class: 'btn btn-success btn-sm' %>
    </div>
  <% end %>
<% end %>

