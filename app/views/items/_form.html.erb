<% if current_user %>
  <h4>Add an Item:</h4>
  <%= form_for [list, list.items.new] do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name, class: 'form-control', placeholder: "Enter To-Do" %>
    <%= f.submit %>
  <% end %>
<% end %>

